import { createUseStyles } from 'react-jss'

export const useStyles = createUseStyles({
    container:{
        marginTop: "1%"
    },

    followerContainer:{
        border: "solid",
        minHeight: "5vh"
    },
    title:{
        fontSize:50,
        textAlign:'left',
        fontWeight:"bold"
    }
})